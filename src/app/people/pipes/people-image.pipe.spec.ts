import { PeopleImagePipe } from './people-image.pipe';

describe('PeopleImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleImagePipe();
    expect(pipe).toBeTruthy();
  });
});
